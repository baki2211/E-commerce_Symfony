<div class="container text-center">
  <div class="row justify-content-center row-cols-4 gap-3 row-gap-3">
  {% for product in products %}

  <div class="col card" style="width: 18rem;">
        <div class="card-title"><h3>{{product.category.name}}</3></div>
        <div class="row row-cols-2">
            <div class="col"> <img src="{{ asset('img/prod.png') }}" class="card-img-top" alt="...">
              <p class="card-text"><small class="text-body-secondary">{{product.name}}</small></p>
            </div>
        <a href="">Shop Now</a>
    </div>
    
  {% endfor %}
    
    </div>
  </div>
  